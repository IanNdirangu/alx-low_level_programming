#include "main.h"
/**
 * _abs - a function that finds the absolute value of an integer
 * @n: the int to be checked
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
