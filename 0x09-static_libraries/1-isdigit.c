#include "main.h"
/**
 * _isdigit - a fuction that looks for a digit (0 through 9)
 * @c: the int to be examined
 * Return: if c is a digit return 1, otherwise return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
