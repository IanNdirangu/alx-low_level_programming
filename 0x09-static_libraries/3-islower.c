#include "main.h"
/**
 * _islower - a function that lookss for lowercase character
 * @c: is the character to examine
 * Return: if c is lowercase return 1, otherwise return 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
